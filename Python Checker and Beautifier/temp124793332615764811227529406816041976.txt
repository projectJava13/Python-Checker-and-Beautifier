#!/usr/bin/python
f surface(lng, lr)->int:
	"""
		sbvusebrub
	"""
	return(lng*lr)
def surface2(lng, lr):
	return(lng*lr)
print(" ? longueur du rectangle : ")
longueur = input()
 
largeur = int(input(" ? largeur du rectangle : "))
print(surface(longueur, largeur))
